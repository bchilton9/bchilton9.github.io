name: Trigger Pushover

on:
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call private pushover relay
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.RELAY_PAT }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/bchilton9/pushover-relay/dispatches \
            -d '{
              "event_type": "pushover",
              "client_payload": {
                "title": "✅ Notification Test",
                "message": "Triggered from '${{ github.repository }}' by ${{ github.actor }}"
              }
            }'
